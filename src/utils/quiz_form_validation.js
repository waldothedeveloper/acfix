const zipCode = /^\d{5,6}(?:[-\s]\d{4})?$/

export const quizValidate = value => {
  let errors = {}

  // if (!values.type_of_project) {
  //   errors.type_of_project = "Please select a type of project"
  // }

  // if (!zipCode.test(values.zipcode_from_user)) {
  //   errors.zipcode_from_user = "Please enter a valid zipcode"
  // }

  return errors
}
